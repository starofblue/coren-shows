tasks:
  - init: npm install && cp webpack_config_override node_modules/react-scripts/config/webpack.config.js && npm run-script build && npm install -g serve && cd build && wget https://corentv.com/corenshows.db && cd .. 
    command: serve -s build
